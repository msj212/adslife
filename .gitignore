# Sensitive config – never commit real credentials
backend/config/config.php
backend/config/firebase-service-account.json
FrontEnd/.env

# Frontend source – not needed on server (only built output is deployed)
FrontEnd/

# Frontend build cache (local only)
FrontEnd/node_modules/
FrontEnd/dist/
FrontEnd/.vite/

# Migration scripts (one-time use)
backend/api/migrate_*.php

# Uploaded media
uploads/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# PHP logs
*.log
.claude/
